name: Deploy Web App

# Declaring custom variables
env:
  SERVER_FOLDER: server
  CLIENT_FOLDER: webpage
  PROJECT_NAME: EurovisionApi.Server
  PUBLISH_FOLDER: publish

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'server/**'
      - 'webpage/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        
      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: ${{ env.CLIENT_FOLDER }}/pnpm-lock.yaml

      - name: Install Angular CLI
        run: npm install -g @angular/cli@22

      - name: Install webpage pnpm packages
        run: pnpm install --frozen-lockfile
        working-directory: ${{ env.CLIENT_FOLDER }}

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Build
        run: dotnet publish $PROJECT_NAME/$PROJECT_NAME.csproj -c Release -o ./$PUBLISH_FOLDER
        working-directory: ${{ env.SERVER_FOLDER }}

      # Turn off deployed service
      - name: Create empty app_offline.html file
        run: |
          mkdir -p ./offline
          touch ./offline/app_offline.html

      - name: Turn off deployed service
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_URL }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          local-dir: ./offline/
          server-dir: /wwwroot/
          dangerous-clean-slate: false

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_URL }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          local-dir: ${{ env.SERVER_FOLDER }}/${{ env.PUBLISH_FOLDER }}/
          server-dir: /wwwroot/
          dangerous-clean-slate: true
